import React, { Component } from "react";
import { BookList } from "../components/common/index"

class Saved extends Component {

    render() {
        return (
            <div className="container">
                <BookList />
            </div>
        )
    }
}


export default Saved;